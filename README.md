aventurapleinair
================

Install ImageMagick: brew install ImageMagic

start postgresql server

run bundle install

run rake db:create:all

run rake db:migrate

run rake db:seed

================
aventurapleinair
