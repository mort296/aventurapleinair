<% title "Top 5 - " %>
<section class="list activities">
	<h2><%= t(:"include.activities") %></h2>
	<ul class="top-locations">
	  <% if @topActivities %>
	    <% l = 0 %>
	    <% @topActivities.each do |top| %>
	      <% if top.online == true %>

	        <li>
	          <% if top.icon_list? %>
	            <%= link_to image_tag(top.icon_list.url(:thumb)) + '<span>'.html_safe + top.name + '</span>'.html_safe, activity_url(if top.slug then top.slug else top.id end) %></li>
	          <% else %>
	            <%= link_to '<span>'.html_safe + top.name + '</span>'.html_safe, activity_url(if top.slug then top.slug else top.id end) %></li>
	          <% end %>
	        <% l = l + 1 %>
	      <% end %>
	    <% end %>
	    <% if l == 0 %>
	      <li><%= t(:"location.no_location") %></li>
	    <% end %>
	  <% else %>
	    <li><%= t(:"location.no_location") %></li>
	  <% end %>
	</ul>
</section>
<section class="list locations">
	<h2><%= t(:locations) %></h2>
	<ul class="top-locations">
	  <% if @topLocations %>
	    <% l = 0 %>
	    <% @topLocations.each do |top| %>
	      <% if top.online == true %>

	        <li data-filter="<% if top.useful_infos %><% top.useful_infos.each do |useful_info| %><%= useful_info.id %> <%  end %><% end %>">
	          <% if top.icon_list? %>
	            <%= link_to image_tag(top.icon_list.url(:thumb)) + '<span>'.html_safe + top.name + '</span>'.html_safe, location_url(if top.slug then top.slug else top.id end) %></li>
	          <% else %>
	            <%= link_to '<span>'.html_safe + top.name + '</span>'.html_safe, location_url(if top.slug then top.slug else top.id end) %></li>
	          <% end %>
	        <% l = l + 1 %>
	      <% end %>
	    <% end %>
	    <% if l == 0 %>
	      <li><%= t(:"location.no_location") %></li>
	    <% end %>
	  <% else %>
	    <li><%= t(:"location.no_location") %></li>
	  <% end %>
	</ul>
</section>